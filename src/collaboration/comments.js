// src/collaboration/comments.js
export function addComment(postId, comment) {
    // Logic to add a comment to a post
    console.log(`Comment added to post ${postId}: ${comment}`);
}

export function mentionUser (username) {
    // Logic to mention a user
    console.log(`User  ${username} has been mentioned.`);
}
